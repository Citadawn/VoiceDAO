---
description: 文档与注释只写最终约定，不写历史过程或迁移叙事
alwaysApply: true
---

# 文档与注释：只写最终约定

更新**项目文档**（如 `README.md`、`CONTRIBUTING.md`等）以及**代码内注释**时，只描述**当前生效、可执行**的约定与行为；默认读者不了解项目过往决策。

## 代码注释的同一标准

- 注释说明**当前逻辑、约束、安全前提、对外契约**；不记录「曾用某表」「曾删某模块」「从 A 迁到 B」等过程性信息。
- 配置项、路径、开关：注释写**含义与用法**（例如「相对 JVM 工作目录」），不写「原先在 classpath 下」类背景。
- 若某段实现依赖另一文件或约定，用**可定位的引用**（路径、函数名、配置键）写清**当前**依赖关系。

## 禁止写法（出现即视为不合规）

- 否定式历史对比：`不再…`、`不要再…`、`不额外…`、`改成…`、`恢复…`、`沿用旧…`。
- 历史分叉：`以前/之前/后来`、`A 改为 B`、`为了兼容旧逻辑`（**例外**：文档标题或章节明确为「迁移指南」「升级步骤」且读者目标就是执行迁移时，允许按步骤写**当前**迁移动作，仍避免流水账式「当初为什么」）。
- 只写禁止项而不写**应当怎么做**的完整正向规则。
- 依赖上下文的指代：`这一版`、`上次改动`、`这次调整`、`上述方案`（无明确名词时）。

## 示例

- **不推荐（流水账）**：「之前我们在标题右侧加了一个『不能为空』提示，后来发现和红色 * 的含义重复就删了；现在改成在 hint 里提示……」
- **推荐（最终约定 + 必要原因）**：「必填为空时：在输入框下方的 hint 位置显示『请输入… + 规则/示例』并标红；规则不符合时：hint 显示具体错误并标红。这样可以减少页面跳动，让用户一眼看到填写规则。」