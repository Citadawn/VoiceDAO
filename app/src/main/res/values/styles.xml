<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 通用按钮样式（主按钮） -->
    <style name="Widget.Button.Primary" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/btn_main_bg</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/sp_16</item>
        <item name="android:minHeight">@dimen/dp_48</item>
    </style>

    <!-- 小号按钮样式 -->
    <style name="Widget.Button.Small" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/btn_main_bg</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/sp_12</item>
        <item name="android:minHeight">@dimen/dp_28</item>
        <item name="android:minWidth">@dimen/dp_44</item>
    </style>
</resources>
