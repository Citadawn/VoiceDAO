---
description: 禁止通过脚本/终端命令直接修改项目文件，所有代码变更必须使用 Cursor 的补丁式编辑
alwaysApply: true
---

# 禁止使用脚本修改项目文件

在本项目中，对源码和配置文件的任何改动，**必须**遵守以下约束：

- **禁止通过脚本或终端命令修改项目文件**
  - 不允许使用 Python、Shell、PowerShell 或其他脚本语言直接读写、重写或批量替换项目文件内容。
  - 不允许使用会原地修改文件的命令（例如 `sed -i`、`perl -pi -e` 等）作用于项目源码目录。
  - 不允许运行“批量生成/覆盖源码文件到现有目录”的脚本或工具。

- **所有代码改动必须使用 Cursor 的补丁机制完成**
  - 使用 Cursor 提供的文件编辑 / 补丁工具（例如逐文件编辑或 ApplyPatch）修改代码。
  - 这样可以通过「Submit from a previous message」重放或回滚对应消息中的修改。

- **终端命令仅限于只读或构建/测试用途**
  - 允许：只读命令（如 `git status`、`git diff`、列目录、查看日志等）。
  - 允许：构建、运行、测试命令（如 Gradle 构建、单元测试等），前提是它们不主动写入/生成源码文件。
  - 如确需运行会写文件的外部工具，**只能由人类用户自行执行并自行管理 Git 回滚**，不要由 AI 通过脚本执行。

- **删除/改名（文件或目录）前：优先建议使用 IDE 操作 + Cursor 影响检查**
  - 原则：在进行**删除**或**改名/移动**文件（或目录）之前，AI 必须先提醒用户：**尽量使用 IDE 的删除/重命名/重构功能**完成，因为 IDE 往往能更好地处理相关联的引用与配置（例如 Kotlin/Java 引用、包名/路径、资源引用、Gradle/Manifest 等）。
  - 若用户仍坚持使用 Cursor 执行上述操作，则 AI 必须在执行前做一次“影响检查”，并在结果中明确说明风险点与将要同步修改的文件：
    - **引用搜索**：在仓库内搜索被删除/改名对象的所有可能引用（文件名、类名、包路径、相对路径、资源名等），列出命中位置。
    - **Android/Gradle 关联点**（按需检查）：`AndroidManifest.xml`、`build.gradle(.kts)`、`settings.gradle`、资源目录 `res/` 中的引用、`assets/` 中的路径引用、Web/JS 侧的路径引用等。
    - **改名/移动的同步更新**：若涉及改名/移动，AI 必须同步更新 imports/package、路径字符串、构建/配置中的引用，避免“文件已改名但引用仍指向旧路径”的断链问题。
    - **删除的安全性**：若涉及删除，AI 必须确认不存在仍在使用的引用；如无法排除风险，必须先提醒用户改用 IDE 重构删除或先做备份/提交。
  - 说明：即使做了影响检查，IDE 的重构能力通常仍优于纯文本替换；因此“Cursor 执行删除/改名”应被视为次选方案。

- **需要批量修改时的处理方式**
  - 优先由 AI 使用补丁方式分步修改文件，而不是通过脚本一次性批量改写。
  - 如确实需要借助脚本生成代码，应输出到临时或新目录，由人类审查后再用补丁方式合并到正式源码中。

遵守以上规则，可以确保本项目中的所有 AI 改动都可以被安全地追踪和回滚。